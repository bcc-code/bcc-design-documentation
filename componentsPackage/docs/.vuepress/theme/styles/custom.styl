.navbar {
  background-color: var(--c-navbar);
  a {
    display: flex;
  }
  .site-name {
    width: -webkit-fill-available;
    position: fixed;
    left: 0px;
    text-align: center;
  }
}
#sidebar {
  background: var(--c-bg);
  border-color: var(--c-border);
}